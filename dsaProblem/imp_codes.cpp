reverse(nums.begin() + pivot + 1,nums.end());

vector<int> ans(n,1); // creat of size n and initilizing all element with 1

if(s.find(trip) == s.end()){ // means this trip doesnot exist already in set s

if(s.find(trip) != s.end()){ // means this trip  exist already in set s

vector<vector<int>>(ansSet.begin(), ansSet.end()) //   Convert the set to a vector of vectors

